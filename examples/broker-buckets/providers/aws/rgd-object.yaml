# kro ResourceGraphDefinition: AWS-Realisierung eines generischen storage.Object.
# Identische Struktur zur GCP-RGD — nur das Realisierungs-Ziel ist floci-aws (S3)
# statt floci-gcp (GCS). Das ist der Kern der Demo: EIN generischer Typ, zwei
# Provider mit je eigener RGD; der Broker routet nach region.
#
# GRENZE (PoC): create-only, wie bei der GCP-RGD. Siehe tasks/todo.md.
apiVersion: kro.run/v1alpha1
kind: ResourceGraphDefinition
metadata:
  name: objects.storage.generic.platform-mesh.io
spec:

  schema:
    group: storage.generic.platform-mesh.io
    apiVersion: v1alpha1
    kind: Object
    spec:
      region: string
      versioning: boolean
    status:
      url: s3://${createBucket.metadata.annotations["spike.odc/bucket-name"]}
      status: ${createBucket.status.succeeded > 0 ? "Available" : "Provisioning"}
      conditions: ${createBucket.status.conditions}

  resources:

    - id: createBucket
      readyWhen:
        - ${createBucket.status.succeeded > 0}
      template:
        apiVersion: batch/v1
        kind: Job
        metadata:
          name: ${schema.metadata.name}-create
          namespace: ${schema.metadata.namespace}
          annotations:
            spike.odc/bucket-name: ${schema.metadata.?annotations["syncagent.kcp.io/remote-object-name"].orValue(schema.metadata.name)}
        spec:
          backoffLimit: 6
          template:
            spec:
              restartPolicy: Never
              enableServiceLinks: false
              containers:
                - name: create-bucket
                  image: amazon/aws-cli:2.31.14
                  env:
                    - name: BUCKET
                      value: ${schema.metadata.?annotations["syncagent.kcp.io/remote-object-name"].orValue(schema.metadata.name)}
                    # floci-aws ignoriert Auth, verlangt aber gesetzte Creds.
                    - name: AWS_ACCESS_KEY_ID
                      value: test
                    - name: AWS_SECRET_ACCESS_KEY
                      value: test
                    - name: AWS_DEFAULT_REGION
                      value: us-east-1
                  command: ["/bin/sh", "-c"]
                  args:
                    - |
                      set -eu
                      echo "creating s3://${BUCKET} in floci-aws"
                      aws --endpoint-url http://floci-aws.floci-aws.svc.cluster.local:4566 \
                        s3api create-bucket --bucket "${BUCKET}"
                      echo "ok"
